---
# defaults file for ansible-role-nextcloud

nextcloud_required_apache_modules:
  - "rewrite"

nextcloud_recommended_apache_modules:
  - "headers"
  - "env"
  - "dir"
  - "mime"

nextcloud_enable_recommended_apache_modules: true
